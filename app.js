"use strict";

var readline = require('./config/readline');
var common = require('./src/common');
var manager = require('./src/task-manager');
var processer = require('./src/command-processer');

(function () {
  common.messages.waitInsert();
  //error
  //split command, verify, process


})()